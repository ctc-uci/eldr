import { Box, Button } from "@chakra-ui/react";

type Props = {};

const CaseView = (props: Props) => {
  return (
    <Box>
      <Button>Send to Volunteer(s)</Button>
    </Box>
  );
};

export default CaseView;
